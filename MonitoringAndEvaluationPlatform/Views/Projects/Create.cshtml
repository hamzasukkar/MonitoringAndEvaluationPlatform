@model MonitoringAndEvaluationPlatform.Models.Project
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@using Microsoft.AspNetCore.Localization

@{
    ViewData["Title"] = "Create New Project";
    Layout = "~/Views/Shared/_DashboardHomeLayout.cshtml";

    // Use model values if available, otherwise default to today's date
    var startDate = Model?.StartDate != default ? Model.StartDate.ToString("yyyy-MM") : DateTime.Today.ToString("yyyy-MM");
    var endDate = Model?.EndDate != default ? Model.EndDate.ToString("yyyy-MM") : DateTime.Today.ToString("yyyy-MM");

    var governorates = ViewBag.Governorates as List<Governorate>;

    // Helper to check if a field has validation errors
    bool HasError(string fieldName) => ViewData.ModelState.ContainsKey(fieldName) && ViewData.ModelState[fieldName].Errors.Count > 0;
    string ValidationClass(string fieldName) => HasError(fieldName) ? "is-invalid" : "";
}

<!-- Dashboard Header -->
<div class="dashboard-header">
    <div class="d-flex justify-content-between align-items-center">
        <div>
            <h1 class="dashboard-title">
                <i class="fas fa-plus-circle me-3"></i>
                @Localizer["Create New Project"]
            </h1>
            <p class="dashboard-subtitle">
                @Localizer["Fill in the project details below to create a comprehensive project plan"]
            </p>
        </div>
        <a asp-action="Index" class="btn btn-outline-secondary btn-lg">
            <i class="fas fa-arrow-left me-2"></i>@Localizer["Back to Projects"]
        </a>
    </div>
</div>

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">
                        <i class="fas fa-edit me-2"></i>
                        @Localizer["Project Information"]
                    </h5>
                </div>
                <div id="main-form-body" class="card-body">
                    <!-- Include notification partial -->
                    <partial name="_Notifications" />

                    @using (Html.BeginForm("Create", "Projects", FormMethod.Post, new { enctype = "multipart/form-data", @class = "needs-validation", novalidate = "novalidate" }))
                    {
                        <!-- Improved validation summary with better styling -->
                        <div id="server-validation-summary" asp-validation-summary="ModelOnly" class="alert alert-danger alert-dismissible fade show" role="alert">
                            <strong><i class="fas fa-exclamation-triangle me-2"></i>@Localizer["Please correct the following errors:"]</strong>
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="@Localizer["Close"]"></button>
                        </div>

                        <div class="row">
                            <!-- Left Column -->
                            <div class="col-md-6">
                                <h6 class="text-primary mb-3">
                                    <i class="fas fa-info-circle me-2"></i>@Localizer["Basic Information"]
                                </h6>

                                <!-- Project Name -->
                                <div class="mb-4 @(HasError("ProjectName") ? "has-validation-error" : "")">
                                    <label asp-for="ProjectName" class="form-label fw-bold">
                                        <i class="fas fa-project-diagram me-2"></i>
                                        @Localizer["Project Name"] <span class="text-danger">*</span>
                                    </label>
                                    <input asp-for="ProjectName" class="form-control form-control-lg @ValidationClass("ProjectName")" placeholder="@Localizer["Enter a descriptive project name (3-200 characters)"]" />
                                    <div class="form-text">@Localizer["Choose a clear and meaningful name for your project"]</div>
                                    <span asp-validation-for="ProjectName" class="invalid-feedback d-block"></span>
                                </div>

                             @*    <!-- Plans Count calculated from project duration -->
                                <div class="mb-4">
                                    <label for="PlansCount" class="form-label fw-bold">
                                        <i class="fas fa-calendar-alt me-2"></i>
                                        @Localizer["Plans Count"] (Months)
                                    </label>
                                    <input type="number"
                                           id="PlansCount"
                                           name="PlansCount"
                                           class="form-control form-control-lg"
                                           value="1"
                                           min="1"
                                           readonly />
                                    <div class="form-text">@Localizer["Automatically calculated based on project duration"]</div>
                                </div> *@

                                <!-- Location Hierarchy -->
                                <div class="mb-4">
                                    <h6 class="text-primary mb-3">
                                        <i class="fas fa-map-marker-alt me-2"></i>@Localizer["Project Location"]
                                    </h6>
                                    <!-- Entire Country Checkbox -->
                                    <div class="mb-3">
                                        <div class="form-check form-switch">
                                            <input class="form-check-input" type="checkbox" id="entireCountryCheckbox" name="IsEntireCountry" value="true" @(Model?.IsEntireCountry == true ? "checked" : "")>
                                            <label class="form-check-label fw-bold" for="entireCountryCheckbox">
                                                <i class="fas fa-flag me-2"></i>@Localizer["This project covers the entire country"]
                                            </label>
                                        </div>
                                        <div class="form-text">@Localizer["Check this if the project is nationwide and not limited to specific locations"]</div>
                                    </div>
                                    <div id="location-selector" class="row g-3 mb-3">
                                        <div class="col-md-6">
                                            <select id="governorate" class="form-select">
                                                <option value="">-- @Localizer["Select Governorate"] --</option>
                                                @if (governorates != null)
                                                {
                                                    @foreach (var gov in governorates)
                                                    {
                                                        var displayName = System.Threading.Thread.CurrentThread.CurrentUICulture.TwoLetterISOLanguageName == "ar"
                                                        ? gov.AR_Name
                                                        : gov.EN_Name;
                                                        <option value="@gov.Code">@displayName</option>
                                                    }
                                                }
                                            </select>
                                        </div>
                                        <div class="col-md-6">
                                            <select id="district" class="form-select">
                                                <option value="">
                                                    -- @Localizer["Select District"] --
                                                </option>
                                            </select>
                                        </div>
                                        <div class="col-md-6">
                                            <select id="subdistrict" class="form-select">
                                                <option value="">-- @Localizer["Select Subdistrict"] --</option>
                                            </select>
                                        </div>
                                        <div class="col-md-6">
                                            <select id="community" class="form-select">
                                                <option value="">-- @Localizer["Select Community"] --</option>
                                            </select>
                                        </div>
                                    </div>
                                    <div class="d-flex gap-2 mb-3">
                                        <button type="button" id="add-location" class="btn btn-primary">
                                            <i class="fas fa-plus me-2"></i>@Localizer["Add Location"]
                                        </button>
                                        <button type="button" id="reset-dropdowns" class="btn btn-outline-secondary">
                                            <i class="fas fa-redo me-2"></i>@Localizer["Reset"]
                                        </button>
                                    </div>
                                    <ul id="selected-locations" class="list-group mb-3"></ul>
                                    <input type="hidden" id="location-data" name="selections" />
                                    <div id="location-validation-alert" style="display:none;" class="alert alert-warning"></div>
                                </div>
                                </div>






                                <!-- Timeline -->
                                <div class="timeline-frame mb-4">
                                    <div class="timeline-frame-header">
                                        <div class="timeline-frame-title">
                                            <i class="fas fa-stream me-2"></i>
                                            @Localizer["Project Timeline"]
                                        </div>
                                        <div class="timeline-frame-dots">
                                            <span class="tl-dot tl-dot-start"></span>
                                            <span class="tl-connector"></span>
                                            <span class="tl-dot tl-dot-end"></span>
                                        </div>
                                    </div>
                                    <div class="timeline-frame-body">
                                        <div class="row g-3">
                                            <div class="col-md-6 @(HasError("StartDate") ? "has-validation-error" : "")">
                                                <div class="tl-date-block tl-start-block">
                                                    <div class="tl-date-icon"><i class="fas fa-play-circle"></i></div>
                                                    <label asp-for="StartDate" class="form-label fw-bold">
                                                        @Localizer["Start Date"] <span class="text-danger">*</span>
                                                    </label>
                                                    <div class="month-year-picker">
                                                        <input type="hidden" name="StartDate" id="StartDate" value="@startDate" />
                                                        <div class="d-flex align-items-center gap-1">
                                                            <select class="form-control mypicker-month" style="max-width:80px">
                                                                @for (int _m = 1; _m <= 12; _m++) { <option value="@_m">@_m</option> }
                                                            </select>
                                                            <span class="text-muted px-1 fw-bold">/</span>
                                                            <input type="number" class="form-control mypicker-year" min="2000" max="2100" style="max-width:100px" placeholder="YYYY" />
                                                        </div>
                                                    </div>
                                                    <div class="form-text">@Localizer["Project implementation start date"]</div>
                                                    <span asp-validation-for="StartDate" class="invalid-feedback d-block"></span>
                                                </div>
                                            </div>
                                            <div class="col-md-6 @(HasError("EndDate") ? "has-validation-error" : "")">
                                                <div class="tl-date-block tl-end-block">
                                                    <div class="tl-date-icon"><i class="fas fa-flag-checkered"></i></div>
                                                    <label asp-for="EndDate" class="form-label fw-bold">
                                                        @Localizer["End Date"] <span class="text-danger">*</span>
                                                    </label>
                                                    <div class="month-year-picker">
                                                        <input type="hidden" name="EndDate" id="EndDate" value="@endDate" />
                                                        <div class="d-flex align-items-center gap-1">
                                                            <select class="form-control mypicker-month" style="max-width:80px">
                                                                @for (int _m = 1; _m <= 12; _m++) { <option value="@_m">@_m</option> }
                                                            </select>
                                                            <span class="text-muted px-1 fw-bold">/</span>
                                                            <input type="number" class="form-control mypicker-year" min="2000" max="2100" style="max-width:100px" placeholder="YYYY" />
                                                        </div>
                                                    </div>
                                                    <div class="form-text">@Localizer["Project completion deadline"]</div>
                                                    <span asp-validation-for="EndDate" class="invalid-feedback d-block"></span>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                </div>
    
                            <!-- Right Column -->
                            <div class="col-md-6">
                                <!-- Budget Information -->
                                <h6 class="text-primary mb-3">
                                    <i class="fas fa-dollar-sign me-2"></i>@Localizer["Budget Information"]
                                </h6>
                                <div class="mb-4 @(HasError("EstimatedBudget") ? "has-validation-error" : "")">
                                    <label asp-for="EstimatedBudget" class="form-label fw-bold">
                                        <i class="fas fa-coins me-2"></i>
                                        @Localizer["Estimated Budget"] <span class="text-danger">*</span>
                                    </label>
                                    <div class="input-group">
                                        <select asp-for="Currency" class="form-select" style="max-width: 100px;" id="currencySelect">
                                            <option value="USD">$ USD</option>
                                            <option value="EUR">&euro; EUR</option>
                                            <option value="SYP">SYP</option>
                                        </select>
                                        <input asp-for="EstimatedBudget" class="form-control form-control-lg @ValidationClass("EstimatedBudget")" min="0.01" step="0.01" placeholder="0.00"
                                               oninvalid="this.setCustomValidity('@Localizer["Please enter a value greater than or equal to 0.01."]')"
                                               oninput="this.setCustomValidity('')" />
                                    </div>
                                    <div class="form-text">@Localizer["Enter the total estimated budget for this project"]</div>
                                    <div id="estimatedBudgetArabicWords" class="form-text mt-1 text-primary fw-bold" dir="rtl" style="display:none;">
                                        <i class="fas fa-language me-1"></i>
                                        <span id="estimatedBudgetArabicWordsValue"></span>
                                    </div>
                                    <span asp-validation-for="EstimatedBudget" class="invalid-feedback d-block"></span>
                                </div>

                                <!-- Exchange Rate (SYP only) -->
                                <div class="mb-4" id="exchangeRateSection" style="display:none;">
                                    <label class="form-label fw-bold">
                                        <i class="fas fa-exchange-alt me-2"></i>
                                        @Localizer["Exchange Rate"] <small class="text-muted">(1 USD = ? SYP)</small>
                                    </label>
                                    <div class="input-group">
                                        <span class="input-group-text">
                                            <i class="fas fa-dollar-sign"></i>
                                        </span>
                                        <input asp-for="ExchangeRate" type="number" class="form-control form-control-lg" id="exchangeRateInput"
                                               min="0.01" step="0.01" placeholder="@Localizer["e.g. 13000"]" />
                                        <span class="input-group-text fw-bold">SYP</span>
                                        <button type="button" class="btn btn-outline-primary" id="fetchRateBtn" title="@Localizer["Fetch current rate"]">
                                            <i class="fas fa-sync-alt me-1"></i>@Localizer["Fetch Rate"]
                                        </button>
                                    </div>
                                    <div id="exchangeRateMeta" class="form-text mt-1" style="display:none;">
                                        <i class="fas fa-calendar-alt me-1 text-muted"></i>
                                        <span id="exchangeRateDateLabel">@Localizer["Rate recorded on"]:</span>
                                        <strong id="exchangeRateDateDisplay"></strong>
                                        <span class="ms-2 badge bg-success" id="exchangeRateSourceBadge" style="display:none;">
                                            <i class="fas fa-check-circle me-1"></i>@Localizer["Live Rate"]
                                        </span>
                                    </div>
                                    <input asp-for="ExchangeRateDate" type="hidden" id="exchangeRateDateInput" />
                                    <div id="fetchRateError" class="text-danger small mt-1" style="display:none;"></div>

                                    <!-- USD Equivalent -->
                                    <div id="usdEquivalentBox" class="mt-3 p-3 rounded-3" style="display:none; background: linear-gradient(135deg, #e8f5e9, #f1f8e9); border: 1px solid #a5d6a7;">
                                        <div class="d-flex align-items-center gap-2">
                                            <i class="fas fa-dollar-sign text-success fs-5"></i>
                                            <span class="text-muted small">@Localizer["Budget in USD"]:</span>
                                            <strong class="text-success fs-5" id="usdEquivalentValue">—</strong>
                                            <span class="text-muted small">USD</span>
                                        </div>
                                        <div class="text-muted" style="font-size: 0.78rem; margin-top: 4px;">
                                            <i class="fas fa-calculator me-1"></i>
                                            <span id="usdEquivalentFormula"></span>
                                        </div>
                                    </div>
                                </div>

                                <!-- Stakeholders -->
                                <h6 class="text-primary mb-3">
                                    <i class="fas fa-users me-2"></i>@Localizer["Stakeholders"]
                                </h6>
                                <div class="mb-4 @(HasError("Sectors") ? "has-validation-error" : "")" id="sectors-container">
                                    <label class="form-label fw-bold">
                                        <i class="fas fa-industry me-2"></i>
                                        @Localizer["Sectors"] <span class="text-danger">*</span>
                                    </label>
                                    <select asp-for="Sectors" class="form-select @ValidationClass("Sectors")" asp-items="ViewBag.SectorList" multiple size="4">
                                        <option disabled>@Localizer["Select one or more sectors"]</option>
                                    </select>
                                    <div class="form-text">@Localizer["Hold Ctrl/Cmd to select multiple sectors"]</div>
                                    <div id="sectors-validation-error" class="invalid-feedback d-block" style="display: none;"></div>
                                </div>

                                <div class="mb-4 @(HasError("ProjectManagerCode") ? "has-validation-error" : "")">
                                    <label asp-for="ProjectManagerCode" class="form-label fw-bold">
                                        <i class="fas fa-user-tie me-2"></i>
                                        @Localizer["Project Manager"] <span class="text-danger">*</span>
                                    </label>
                                    <select asp-for="ProjectManagerCode" class="form-select form-select-lg @ValidationClass("ProjectManagerCode")" asp-items="ViewBag.ProjectManager">
                                        <option value="">-- @Localizer["Select Project Manager"] --</option>
                                    </select>
                                <div class="form-text">@Localizer["Choose the primary manager responsible for this project"]</div>
                                    <span asp-validation-for="ProjectManagerCode" class="invalid-feedback d-block"></span>
                                </div>

                                <div class="mb-4 @(HasError("SuperVisorCode") ? "has-validation-error" : "")">
                                    <label asp-for="SuperVisorCode" class="form-label fw-bold">
                                        <i class="fas fa-user-check me-2"></i>
                                        @Localizer["Supervisor"] <span class="text-danger">*</span>
                                    </label>
                                    <select asp-for="SuperVisorCode" class="form-select form-select-lg @ValidationClass("SuperVisorCode")" asp-items="ViewBag.SuperVisor">
                                        <option value="">-- @Localizer["Select Supervisor"] --</option>
                                    </select>
                                <div class="form-text">@Localizer["Choose the supervisor who will oversee this project"]</div>
                                    <span asp-validation-for="SuperVisorCode" class="invalid-feedback d-block"></span>
                                </div>

                                <div class="mb-4 @(HasError("MinistryCode") ? "has-validation-error" : "")">
                                    <label asp-for="MinistryCode" class="form-label fw-bold">
                                        <i class="fas fa-building me-2"></i>
                                        @Localizer["Ministry"] <span class="text-danger">*</span>
                                    </label>
                                    <div class="dropdown ministry-dropdown">
                                        <button class="btn btn-outline-secondary dropdown-toggle form-control form-control-lg text-start d-flex align-items-center justify-content-between @(HasError("MinistryCode") ? "is-invalid" : "")" type="button" id="ministryDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                                            <span id="selectedMinistryDisplay" class="text-muted">@Localizer["Select Ministry"]</span>
                                        </button>
                                        <ul class="dropdown-menu dropdown-menu-lg w-100" aria-labelledby="ministryDropdown" style="max-height: 400px; overflow-y: auto;">
                                            <li class="ministry-search-wrapper px-2 pt-2 pb-1 sticky-top bg-white">
                                                <div class="input-group input-group-sm">
                                                    <span class="input-group-text border-end-0 bg-white">
                                                        <i class="fas fa-search text-muted"></i>
                                                    </span>
                                                    <input type="text" id="ministrySearch" class="form-control border-start-0"
                                                           placeholder="@Localizer["Search ministry..."]"
                                                           autocomplete="off" />
                                                </div>
                                                <div id="ministryNoResults" class="text-center text-muted py-2 small" style="display:none;">
                                                    <i class="fas fa-search me-1"></i>@Localizer["No ministries found"]
                                                </div>
                                            </li>
                                            <li><hr class="dropdown-divider mt-0"></li>
                                            @if (ViewBag.Ministries != null)
                                            {
                                                var isArabic = System.Threading.Thread.CurrentThread.CurrentUICulture.TwoLetterISOLanguageName == "ar";
                                                var ministries = ViewBag.Ministries as List<Ministry>;
                                                @foreach (var ministry in ministries)
                                                {
                                                    var ministryName = isArabic ? ministry.MinistryDisplayName_AR : ministry.MinistryDisplayName_EN;
                                                    <li>
                                                        <a class="dropdown-item ministry-item d-flex align-items-center py-2" href="#" data-ministry-code="@ministry.Code" data-ministry-name="@ministryName" data-ministry-logo="@ministry.Logo">
                                                            @if (!string.IsNullOrEmpty(ministry.Logo))
                                                            {
                                                                <span class="ministry-logo me-2 d-flex align-items-center justify-content-center" style="width: 30px; height: 30px; font-size: 1.5rem;">@ministry.Logo</span>
                                                            }
                                                            else
                                                            {
                                                                <div class="ministry-logo-placeholder me-2 bg-light border rounded d-flex align-items-center justify-content-center" style="width: 30px; height: 30px;">
                                                                    <i class="fas fa-building text-muted" style="font-size: 0.8rem;"></i>
                                                                </div>
                                                            }
                                                            <span>@ministryName</span>
                                                        </a>
                                                    </li>
                                                }
                                            }
                                        </ul>
                                    </div>
                                    <input type="hidden" asp-for="MinistryCode" id="ministrySelect" />
                                    <span asp-validation-for="MinistryCode" class="invalid-feedback d-block"></span>
                                </div>

                                <div class="mb-4">
                                    <label asp-for="GoalCode" class="form-label fw-bold">@Localizer["SDGs"]</label>
                                    <select asp-for="GoalCode" class="form-select form-select-lg" asp-items="ViewBag.Goals">
                                        <option value="">@Localizer["SDGs"]</option>
                                    </select>
                                </div>

                                <!-- Donor Selection -->
                                <div class="mb-4">
                                    <label class="form-label fw-bold">@Localizer["Available Donors"]</label>
                                    <div class="row g-3">
                                        <div class="col-md-8">
                                            <select class="form-select form-select-lg" id="availableDonorSelect">
                                                <option value="">@Localizer["Select a donor to add"]</option>
                                                @if (ViewBag.Donor != null)
                                                {
                                                    var donorSelectList = ViewBag.Donor as SelectList;
                                                    var investmentCodes = ViewBag.InvestmentBudgetDonorCodes as List<string> ?? new List<string>();
                                                    if (donorSelectList != null)
                                                    {
                                                        @foreach (var item in donorSelectList)
                                                        {
                                                            if (investmentCodes.Contains(item.Value))
                                                            {
                                                                <option value="@item.Value" data-investment-budget="true">@item.Text</option>
                                                            }
                                                            else
                                                            {
                                                                <option value="@item.Value">@item.Text</option>
                                                            }
                                                        }
                                                    }
                                                }
                                            </select>
                                        </div>
                                        <div class="col-md-4">
                                            <button type="button" id="addDonorBtn" class="btn btn-primary btn-lg w-100">
                                                <i class="fas fa-plus me-2"></i>@Localizer["Add Donor"]
                                            </button>
                                        </div>
                                    </div>
                                    <div class="form-text">@Localizer["Select donors one by one and specify their funding percentages"]</div>
                                </div>

                                <!-- Selected Donors List (Hidden input for form submission) -->
                                <select asp-for="Donors" multiple style="display: none;" id="selectedDonorsInput"></select>

                                <!-- Donor Funding Breakdown -->
                                <div id="donorFundingSection" style="display: none;">
                                    <h6 class="text-primary mb-3">
                                        <i class="fas fa-percentage me-2"></i>@Localizer["Donor Funding Breakdown"]
                                    </h6>
                                    <div class="alert alert-info">
                                        <i class="fas fa-info-circle me-2"></i>
                                        @Localizer["Enter percentage directly or use sliders to specify the funding for each donor. Total cannot exceed 100%."]
                                    </div>

                                    <div id="donorContributionList" class="mb-4"></div>

                                    <div class="row g-3 mb-4">
                                        <div class="col-md-6">
                                            <div class="card bg-light">
                                                <div class="card-body">
                                                    <h6 class="card-title mb-2">
                                                        <i class="fas fa-calculator me-2"></i>@Localizer["Total Percentage"]
                                                    </h6>
                                                    <div class="d-flex align-items-center">
                                                        <span id="totalPercentage" class="h4 mb-0 me-2">0.00</span>
                                                        <span class="text-muted">%</span>
                                                    </div>
                                                    <small id="percentageStatus" class="text-muted">@Localizer["Cannot exceed 100%"]</small>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="card bg-light">
                                                <div class="card-body">
                                                    <h6 class="card-title mb-2">
                                                        <i class="fas fa-money-bill-wave me-2"></i>@Localizer["Total Funding Amount"]
                                                    </h6>
                                                    <div class="d-flex align-items-center">
                                                        <span class="me-1 currency-symbol">$</span>
                                                        <span id="totalFundingAmount" class="h4 mb-0">0.00</span>
                                                    </div>
                                                    <small class="text-muted">@Localizer["Based on estimated budget"]</small>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <input type="hidden" id="donorFundingData" name="DonorFundingBreakdown" />
                                    <input type="hidden" name="LinkedIndicatorId" value="@ViewBag.PreSelectedIndicatorId" />
                                </div>

                                    </div>

                        <!-- Phase Selection (visible only when "موازنة أستثمارية" donor is selected) -->
                        <div class="mb-4" id="phaseSelectionSection" style="display:none">
                            <label class="form-label fw-bold">
                                <i class="fas fa-tasks me-2"></i>
                                @Localizer["Project Phases"] <span class="text-danger">*</span>
                            </label>
                            <div class="form-text mb-2">@Localizer["Select the phases to include in this project (at least one required)"]</div>
                            <div id="phaseCheckboxesWrapper">
                            @{
                                var isArabicCulture = System.Globalization.CultureInfo.CurrentUICulture.TwoLetterISOLanguageName == "ar";
                                var phaseArNames = new string[]
                                {
                                    "اراضي",
                                    "مباني وانشاءات ومرافق",
                                    "الات ومعدات",
                                    "وسائل نقل وانتقال",
                                    "عدد وادوات وقوالب",
                                    "اثاث ومعدات مكاتب",
                                    "ثروة حيوانية",
                                    "نفقات تاسيس",
                                    "رواتب واجور وتعويضات"
                                };
                                var phaseEnNames = new string[]
                                {
                                    "Land",
                                    "Buildings, Constructions & Facilities",
                                    "Machinery & Equipment",
                                    "Transportation Means",
                                    "Tools, Instruments & Molds",
                                    "Furniture & Office Equipment",
                                    "Livestock",
                                    "Establishment Expenses",
                                    "Salaries, Wages & Compensations"
                                };
                            }
                            <div class="mb-2">
                                <button type="button" id="selectAllPhases" class="btn btn-sm btn-outline-secondary">
                                    <i class="fas fa-check-double me-1"></i>@Localizer["Select All"]
                                </button>
                                <button type="button" id="deselectAllPhases" class="btn btn-sm btn-outline-secondary ms-2">
                                    <i class="fas fa-times me-1"></i>@Localizer["Deselect All"]
                                </button>
                            </div>
                            <div class="row g-2 p-3 border rounded bg-light">
                                @for (int pi = 0; pi < phaseArNames.Length; pi++)
                                {
                                    <div class="col-md-4 col-6">
                                        <label class="d-flex align-items-center gap-2 mb-0"
                                               style="cursor:pointer; width:fit-content;"
                                               for="phase_@pi">
                                            <input class="phase-checkbox flex-shrink-0"
                                                   type="checkbox"
                                                   name="SelectedPhases"
                                                   value="@phaseArNames[pi]"
                                                   id="phase_@pi"
                                                   style="width:1.1em; height:1.1em; cursor:pointer;" />
                                            <span class="small">@(isArabicCulture ? phaseArNames[pi] : phaseEnNames[pi])</span>
                                        </label>
                                    </div>
                                }
                            </div>
                            </div><!-- end phaseCheckboxesWrapper -->
                            @if (ViewData.ModelState.ContainsKey("SelectedPhases") && ViewData.ModelState["SelectedPhases"]!.Errors.Count > 0)
                            {
                                <div class="text-danger small mt-1">
                                    <i class="fas fa-exclamation-triangle me-1"></i>
                                    @ViewData.ModelState["SelectedPhases"]!.Errors[0].ErrorMessage
                                </div>
                            }
                        </div>

                                <!-- Files Upload -->
                                <h6 class="text-primary mb-3">
                                    <i class="fas fa-file-upload me-2"></i>@Localizer["Project Documents"]
                                </h6>
                                <div class="mb-4">
                                    <label class="form-label fw-bold">@Localizer["Upload Files"]</label>
                                    <div class="file-upload-wrapper">
                                        <div class="file-upload-area" id="fileUploadArea">
                                            <i class="fas fa-cloud-upload-alt fa-3x text-muted mb-3"></i>
                                            <p class="mb-2">@Localizer["Drag and drop files here or click to browse"]</p>
                                            <input type="file" name="UploadedFiles" id="fileInput" multiple class="file-input-hidden" accept=".pdf,.doc,.docx,.xls,.xlsx,.jpg,.jpeg,.png" />
                                            <button type="button" class="btn btn-outline-primary" onclick="document.getElementById('fileInput').click()">
                                                <i class="fas fa-folder-open me-2"></i>@Localizer["Browse Files"]
                                            </button>
                                        </div>
                                        <div id="selectedFilesList" class="selected-files-list mt-3"></div>
                                    </div>
                                    <div class="form-text">@Localizer["Maximum file size: 10MB. Allowed types: PDF, DOC, XLS, JPG, PNG"]</div>
                                </div>

                        <!-- Form Actions -->
                        <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4 pt-4 border-top">
                            <a href="@Url.Action("Index")" class="btn btn-outline-secondary btn-lg px-4">
                                <i class="fas fa-arrow-left me-2"></i>@Localizer["Cancel"]
                            </a>
                            <button type="submit" class="btn btn-primary btn-lg px-4">
                                <i class="fas fa-save me-2"></i>@Localizer["Create Project"]
                            </button>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

<partial name="_ValidationScriptsPartial" />

<!-- Standard Styles -->
<style>
    .card {
        border-radius: 0.5rem;
        border: none;
    }

    .form-control-lg, .form-select-lg {
        padding: 0.75rem 1rem;
        font-size: 1.05rem;
    }

    .form-control[readonly] {
        background-color: #f8f9fa;
        opacity: 1;
    }

    .list-group-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    /* Validation Error Highlighting Styles */
    .has-validation-error {
        animation: shake 0.5s ease-in-out;
        border-radius: 8px;
        padding: 10px;
        margin: -10px;
        margin-bottom: 1rem;
        background-color: rgba(220, 53, 69, 0.05);
        border-left: 4px solid #dc3545;
    }

    .has-validation-error label {
        color: #dc3545;
    }

    .is-invalid {
        border-color: #dc3545 !important;
        background-color: rgba(220, 53, 69, 0.05) !important;
        box-shadow: 0 0 0 0.2rem rgba(220, 53, 69, 0.25) !important;
    }

    .is-invalid:focus {
        border-color: #dc3545 !important;
        box-shadow: 0 0 0 0.25rem rgba(220, 53, 69, 0.25) !important;
    }

    .invalid-feedback {
        color: #dc3545;
        font-size: 0.875rem;
        margin-top: 0.25rem;
        font-weight: 500;
    }

    @@keyframes shake {
        0%, 100% { transform: translateX(0); }
        10%, 30%, 50%, 70%, 90% { transform: translateX(-5px); }
        20%, 40%, 60%, 80% { transform: translateX(5px); }
    }

    /* Scroll to first error on page load */
    .has-validation-error:first-of-type {
        scroll-margin-top: 120px;
    }

    /* Project Timeline Frame */
    .timeline-frame {
        position: relative;
        border-radius: 16px;
        background: linear-gradient(135deg, #f0f7ff 0%, #e8f4ff 100%);
        border: 1.5px solid #b8d4f8;
        overflow: hidden;
        box-shadow: 0 4px 16px rgba(0, 123, 255, 0.1);
    }

    .timeline-frame-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0.75rem 1.25rem;
        background: linear-gradient(90deg, #007bff 0%, #0056b3 100%);
        color: white;
    }

    .timeline-frame-title {
        font-size: 0.95rem;
        font-weight: 700;
        letter-spacing: 0.3px;
    }

    .timeline-frame-dots {
        display: flex;
        align-items: center;
        gap: 0;
    }

    .tl-dot {
        width: 12px;
        height: 12px;
        border-radius: 50%;
        border: 2px solid white;
        display: inline-block;
        flex-shrink: 0;
    }

    .tl-dot-start {
        background: #10b981;
        box-shadow: 0 0 6px rgba(16, 185, 129, 0.8);
    }

    .tl-dot-end {
        background: #ef4444;
        box-shadow: 0 0 6px rgba(239, 68, 68, 0.8);
    }

    .tl-connector {
        width: 48px;
        height: 2px;
        background: repeating-linear-gradient(90deg, rgba(255,255,255,0.8) 0, rgba(255,255,255,0.8) 4px, transparent 4px, transparent 8px);
        display: inline-block;
        margin: 0 6px;
    }

    .timeline-frame-body {
        padding: 1.25rem 1.25rem 1rem;
    }

    .tl-date-block {
        position: relative;
        background: white;
        border-radius: 12px;
        padding: 1rem 1rem 0.75rem;
        border-top: 3px solid transparent;
        box-shadow: 0 2px 8px rgba(0,0,0,0.06);
        transition: box-shadow 0.2s ease;
    }

    .tl-date-block:hover {
        box-shadow: 0 4px 16px rgba(0,0,0,0.1);
    }

    .tl-start-block {
        border-top-color: #10b981;
    }

    .tl-end-block {
        border-top-color: #ef4444;
    }

    .tl-date-icon {
        position: absolute;
        top: -14px;
        left: 1rem;
        width: 28px;
        height: 28px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.85rem;
        color: white;
        box-shadow: 0 2px 8px rgba(0,0,0,0.2);
    }

    .tl-start-block .tl-date-icon {
        background: linear-gradient(135deg, #10b981, #059669);
    }

    .tl-end-block .tl-date-icon {
        background: linear-gradient(135deg, #ef4444, #dc2626);
    }

    .tl-date-block label {
        margin-top: 0.25rem;
        font-size: 0.85rem;
        color: #374151;
    }

    .donor-funding-card {
        border: 1px solid #e3e6f0;
        transition: all 0.3s ease;
    }

    .donor-funding-card:hover {
        box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.15);
        border-color: #5a5c69;
    }

    .donor-amount-input {
        font-size: 0.9rem;
        font-weight: 600;
        color: #5a5c69;
    }

    #donorFundingSection .card.bg-light {
        border: 2px solid #e3e6f0;
    }

    #percentageStatus.text-success {
        font-weight: 600;
    }

    #percentageStatus.text-warning {
        font-weight: 600;
    }

    #percentageStatus.text-danger {
        font-weight: 600;
    }

    /* Enhanced slider styling */
    .donor-slider {
        cursor: pointer;
    }

    .donor-slider::-webkit-slider-thumb {
        background: #007bff;
        border: 2px solid #007bff;
        height: 20px;
        width: 20px;
        border-radius: 50%;
        cursor: pointer;
        -webkit-appearance: none;
        box-shadow: 0 2px 4px rgba(0, 123, 255, 0.3);
    }

    .donor-slider::-webkit-slider-track {
        background: #e9ecef;
        height: 6px;
        border-radius: 3px;
    }

    .donor-slider::-moz-range-thumb {
        background: #007bff;
        border: 2px solid #007bff;
        height: 20px;
        width: 20px;
        border-radius: 50%;
        cursor: pointer;
        box-shadow: 0 2px 4px rgba(0, 123, 255, 0.3);
    }

    .donor-slider::-moz-range-track {
        background: #e9ecef;
        height: 6px;
        border-radius: 3px;
    }

    /* Percentage input styling */
    .donor-percentage-input {
        text-align: center;
        font-weight: 600;
    }

    .donor-percentage-input::-webkit-inner-spin-button,
    .donor-percentage-input::-webkit-outer-spin-button {
        opacity: 1;
    }

    .percentage-display {
        font-weight: 600;
        color: #007bff;
    }

    .remove-donor-btn {
        border-radius: 50%;
        width: 32px;
        height: 32px;
        padding: 0;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .remove-donor-btn:hover {
        background-color: #dc3545;
        border-color: #dc3545;
        color: white;
    }

    @@media (max-width: 768px) {
        .d-md-flex {
            flex-direction: column !important;
        }
        .justify-content-md-end {
            align-items: stretch !important;
        }
        .donor-funding-card .col-md-3,
        .donor-funding-card .col-md-6 {
            margin-bottom: 10px;
        }
    }

    /* File Upload Styles */
    .file-upload-wrapper {
        width: 100%;
    }

    .file-upload-area {
        border: 2px dashed #dee2e6;
        border-radius: 8px;
        padding: 30px;
        text-align: center;
        background-color: #f8f9fa;
        transition: all 0.3s ease;
        cursor: pointer;
    }

    .file-upload-area:hover,
    .file-upload-area.dragover {
        border-color: #007bff;
        background-color: #e7f1ff;
    }

    .file-input-hidden {
        display: none;
    }

    .selected-files-list {
        max-height: 300px;
        overflow-y: auto;
    }

    .file-item {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 10px 15px;
        background-color: #fff;
        border: 1px solid #dee2e6;
        border-radius: 6px;
        margin-bottom: 8px;
        transition: all 0.2s ease;
    }

    .file-item:hover {
        background-color: #f8f9fa;
        border-color: #adb5bd;
    }

    .file-item-info {
        display: flex;
        align-items: center;
        flex: 1;
        min-width: 0;
    }

    .file-item-icon {
        width: 40px;
        height: 40px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 6px;
        margin-right: 12px;
        font-size: 1.2rem;
    }

    .file-item-icon.pdf { background-color: #fee2e2; color: #dc2626; }
    .file-item-icon.doc { background-color: #dbeafe; color: #2563eb; }
    .file-item-icon.xls { background-color: #dcfce7; color: #16a34a; }
    .file-item-icon.img { background-color: #fef3c7; color: #d97706; }
    .file-item-icon.default { background-color: #e5e7eb; color: #6b7280; }

    .file-item-details {
        flex: 1;
        min-width: 0;
    }

    .file-item-name {
        font-weight: 500;
        color: #1f2937;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    .file-item-size {
        font-size: 0.85rem;
        color: #6b7280;
    }

    .file-item-remove {
        background: none;
        border: none;
        color: #dc3545;
        cursor: pointer;
        padding: 5px 10px;
        border-radius: 4px;
        transition: all 0.2s ease;
    }

    .file-item-remove:hover {
        background-color: #fee2e2;
    }

    .files-summary {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 10px 15px;
        background-color: #e7f1ff;
        border-radius: 6px;
        margin-bottom: 10px;
    }

    .files-summary-text {
        font-weight: 500;
        color: #1e40af;
    }

    .clear-all-btn {
        font-size: 0.875rem;
    }
</style>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script>
    // Localized strings for JavaScript
    const localizedStrings = {
        select: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["Select"].Value))',
        clear: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["Clear"].Value))',
        pleaseSelectAllLocationLevels: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["Please select all location levels."].Value))',
        locationAlreadyAdded: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["This location has already been added."].Value))',
        thisProjectWillCover: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["This project will cover all locations in the country. Specific location selection is disabled."].Value))',
        ministryAutoAssigned: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["Ministry is automatically assigned based on your account"].Value))',
        pleaseSelectDonorFirst: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["Please select a donor first."].Value))',
        donorAlreadyAdded: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["This donor has already been added."].Value))',
        fundingContribution: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["Funding Contribution"].Value))',
        percentage: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["Percentage:"].Value))',
        amount: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["Amount:"].Value))',
        fullyFunded: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["Fully funded"].Value))',
        partiallyFunded: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["Partially funded"].Value))',
        cannotExceed100: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["Cannot exceed 100%"].Value))',
        atLeastOneLocation: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["At least one project location must be selected, or check the Entire Country option."].Value))',
        atLeastOneSector: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["At least one sector must be selected."].Value))',
projectManagerMustBeSelected: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["A project manager must be selected."].Value))',
        supervisorMustBeSelected: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["A supervisor must be selected."].Value))',
        ministryMustBeSelected: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["A ministry must be selected."].Value))',
        endDateMustBeAfterStart: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["End date must be after start date."].Value))',
        donorFundingCannotExceed: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["Total donor funding percentage cannot exceed 100%."].Value))',
        pleaseFixFollowingIssues: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["Please fix the following issues:"].Value))',
        close: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["Close"].Value))',
        filesSelected: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["file(s) selected"].Value))',
        clearAll: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["Clear All"].Value))',
        remove: '@Html.Raw(System.Text.Encodings.Web.JavaScriptEncoder.Default.Encode(Localizer["Remove"].Value))'
    };

    let selections = [];

    // Handle Entire Country checkbox
    $("#selectAllPhases").click(function() {
        $(".phase-checkbox").prop("checked", true);
    });
    $("#deselectAllPhases").click(function() {
        $(".phase-checkbox").prop("checked", false);
    });

    $("#entireCountryCheckbox").change(function() {
        const isChecked = $(this).is(':checked');

        if (isChecked) {
            // Disable location selectors
            $("#location-selector select").prop('disabled', true).val('');
            $("#add-location, #reset-dropdowns").prop('disabled', true);

            // Clear any selected locations
            selections = [];
            $("#selected-locations").empty();
            $("#location-data").val('');

            // Show info message
            if ($("#entire-country-info").length === 0) {
                $("#location-selector").before(
                    '<div id="entire-country-info" class="alert alert-info">' +
                    '<i class="fas fa-info-circle me-2"></i>' + localizedStrings.thisProjectWillCover +
                    '</div>'
                );
            }
        } else {
            // Enable location selectors
            $("#location-selector select").prop('disabled', false);
            $("#add-location, #reset-dropdowns").prop('disabled', false);

            // Remove info message
            $("#entire-country-info").remove();
        }
    });

    // Cascading dropdowns
    $("#governorate").change(function () {
        const selectedValue = $(this).val();

        if (selectedValue) {
            $.get("/Projects/GetDistricts", { governorateCode: selectedValue }, function (data) {
                fillSelect("#district", data);
                resetSelect("#subdistrict");
                resetSelect("#community");
            });
        } else {
            resetSelect("#district");
            resetSelect("#subdistrict");
            resetSelect("#community");
        }
    });

    $("#district").change(function () {
        $.get("/Projects/GetSubDistricts", { districtCode: $(this).val() }, function (data) {
            fillSelect("#subdistrict", data);
            resetSelect("#community");
        });
    });

    $("#subdistrict").change(function () {
        $.get("/Projects/GetCommunities", { subDistrictCode: $(this).val() }, function (data) {
            fillSelect("#community", data);
        });
    });

    // Add location
    $("#add-location").click(function () {
        const govCode = $("#governorate").val();
        const govName = $("#governorate option:selected").text();

        const distCode = $("#district").val();
        const distName = $("#district option:selected").text();

        const subCode = $("#subdistrict").val();
        const subName = $("#subdistrict option:selected").text();

        const commCode = $("#community").val();
        const commName = $("#community option:selected").text();

        if (!govCode || !distCode || !subCode || !commCode) {
            showValidationMessage(localizedStrings.pleaseSelectAllLocationLevels);
            return;
        }

        // Check for duplicates
        const exists = selections.some(x =>
            x.GovernorateCode === govCode &&
            x.DistrictCode === distCode &&
            x.SubDistrictCode === subCode &&
            x.CommunityCode === commCode
        );

        if (exists) {
            showValidationMessage(localizedStrings.locationAlreadyAdded);
            return;
        }

        // Add location with names for display
        const item = {
            GovernorateCode: govCode,
            GovernorateName: govName,
            DistrictCode: distCode,
            DistrictName: distName,
            SubDistrictCode: subCode,
            SubDistrictName: subName,
            CommunityCode: commCode,
            CommunityName: commName
        };

        selections.push(item);

        // Create list item with "Clear" button
        const $li = $(
            '<li class="list-group-item">' +
                '<span>' + govName + ' > ' + distName + ' > ' + subName + ' > ' + commName + '</span>' +
                '<button type="button" class="btn btn-sm btn-outline-danger btn-clear">' +
                    '<i class="fas fa-times"></i> ' + localizedStrings.clear +
                '</button>' +
            '</li>'
        );

        // Attach remove handler
        $li.find(".btn-clear").click(function () {
            // Remove from selections array
            selections = selections.filter(x =>
                !(x.GovernorateCode === govCode &&
                  x.DistrictCode === distCode &&
                  x.SubDistrictCode === subCode &&
                  x.CommunityCode === commCode)
            );
            // Remove from DOM
            $li.remove();
            // Update hidden input
            $("#location-data").val(JSON.stringify(selections));
        });

        $("#selected-locations").append($li);
        $("#location-data").val(JSON.stringify(selections));

        resetDropdowns();
    });

    // Reset handler
    $("#reset-dropdowns").click(resetDropdowns);

    // Utility functions
    function fillSelect(selector, data) {
        let select = $(selector);
        select.empty().append('<option value="">-- ' + localizedStrings.select + ' --</option>');
        data.forEach(x => select.append('<option value="' + x.code + '">' + x.name + '</option>'));
    }

    function resetSelect(selector) {
        $(selector).empty().append('<option value="">-- ' + localizedStrings.select + ' --</option>');
    }

    function resetDropdowns() {
        $("#governorate").val("");
        resetSelect("#district");
        resetSelect("#subdistrict");
        resetSelect("#community");
    }

    // Validation message display
    function showValidationMessage(message, type = 'warning') {
        const alertBox = $("#location-validation-alert");
        let iconClass, alertClass;

        switch(type) {
            case 'success':
                iconClass = 'fa-check-circle';
                alertClass = 'alert-success';
                break;
            case 'info':
                iconClass = 'fa-info-circle';
                alertClass = 'alert-info';
                break;
            default:
                iconClass = 'fa-exclamation-triangle';
                alertClass = 'alert-warning';
        }

        if (alertBox.length === 0) {
            $("#location-selector").after(`
                <div id="location-validation-alert" class="alert ${alertClass} mt-3">
                    <i class="fas ${iconClass} me-2"></i>${message}
                </div>
            `);
        } else {
            alertBox.removeClass('alert-warning alert-success alert-info').addClass(alertClass);
            alertBox.html(`<i class="fas ${iconClass} me-2"></i>${message}`).show();
        }

        // Don't auto-hide info messages (processing messages)
        if (type !== 'info') {
            setTimeout(() => $("#location-validation-alert").fadeOut(), 3000);
        }
    }

    // Old validation message display (keeping for compatibility)
    function showValidationMessage_old(message) {
        const alertBox = $("#location-validation-alert");

        if (alertBox.length === 0) {
            $("#location-selector").after(`
                <div id="location-validation-alert" class="alert alert-warning mt-3">
                    <i class="fas fa-exclamation-triangle me-2"></i>${message}
                </div>
            `);
        } else {
            alertBox.html(`<i class="fas fa-exclamation-triangle me-2"></i>${message}`).show();
        }

        setTimeout(() => $("#location-validation-alert").fadeOut(), 3000);
    }


    // Calculate plans count based on project duration
    function calculatePlansCount() {
        const startDate = new Date($('[name="StartDate"]').val());
        const endDate = new Date($('[name="EndDate"]').val());
        
        if (startDate && endDate && endDate > startDate) {
            // Calculate months between dates
            const months = (endDate.getFullYear() - startDate.getFullYear()) * 12 + 
                          (endDate.getMonth() - startDate.getMonth()) + 1;
            $('#PlansCount').val(Math.max(1, months));
        } else {
            $('#PlansCount').val(1);
        }
    }

    // Update plans count when dates change
    $('[name="StartDate"], [name="EndDate"]').on('change', calculatePlansCount);

    // Calculate initial plans count on page load
    $(document).ready(function() {
        calculatePlansCount();

        // Restore preserved form data after validation failure
        restoreFormData();

        // Handle initial state of "Entire Country" checkbox
        if ($("#entireCountryCheckbox").is(':checked')) {
            $("#location-selector select").prop('disabled', true).val('');
            $("#add-location, #reset-dropdowns").prop('disabled', true);
            if ($("#entire-country-info").length === 0) {
                $("#location-selector").before(
                    '<div id="entire-country-info" class="alert alert-info">' +
                    '<i class="fas fa-info-circle me-2"></i>' + localizedStrings.thisProjectWillCover +
                    '</div>'
                );
            }
        }

        // Scroll to first validation error if any
        var $firstError = $('.has-validation-error').first();
        if ($firstError.length > 0) {
            $('html, body').animate({
                scrollTop: $firstError.offset().top - 120
            }, 500);

            // Focus on the first invalid input
            $firstError.find('.is-invalid').first().focus();
        }

        @if (!string.IsNullOrEmpty(ViewBag.PreFilledProjectName))
        {
            <text>
            // The project name is already set in the model, but we can add a visual indicator
            // Highlight the project name field briefly to draw attention
            $('input[name="ProjectName"]').addClass('border-success border-2');
            setTimeout(function() {
                $('input[name="ProjectName"]').removeClass('border-success border-2');
            }, 2000);
            </text>
        }

        // Handle ministry dropdown selection
        $('.ministry-item').click(function(e) {
            e.preventDefault();
            const ministryCode = $(this).data('ministry-code');
            const ministryName = $(this).data('ministry-name');
            const ministryLogo = $(this).data('ministry-logo');

            // Update hidden input
            $('#ministrySelect').val(ministryCode);

            // Update display button
            updateMinistryDisplay(ministryCode, ministryName, ministryLogo);

            // Close dropdown
            $('#ministryDropdown').dropdown('hide');
        });

        // Ministry search — prevent dropdown from closing while typing
        $(document).on('click', '#ministrySearch', function(e) {
            e.stopPropagation();
        });

        $(document).on('input', '#ministrySearch', function() {
            const query = $(this).val().toLowerCase().trim();
            let visibleCount = 0;

            $('.ministry-item').each(function() {
                const name = ($(this).data('ministry-name') || '').toLowerCase();
                const match = !query || name.includes(query);
                $(this).closest('li').toggle(match);
                if (match) visibleCount++;
            });

            $('#ministryNoResults').toggle(visibleCount === 0);
        });

        // Clear search when dropdown closes
        $('#ministryDropdown').closest('.dropdown').on('hidden.bs.dropdown', function() {
            $('#ministrySearch').val('');
            $('.ministry-item').closest('li').show();
            $('#ministryNoResults').hide();
        });

        // Function to update ministry display
        function updateMinistryDisplay(ministryCode, ministryName, ministryLogo) {
            $('#selectedMinistryDisplay').html('').removeClass('text-muted');
            if (ministryLogo && ministryLogo.trim() !== '') {
                $('#selectedMinistryDisplay').append('<span class="me-2" style="font-size: 1.5rem;">' + ministryLogo + '</span>');
            } else {
                $('#selectedMinistryDisplay').append('<span class="me-2 bg-light border rounded d-flex align-items-center justify-content-center" style="width: 24px; height: 24px; display: inline-flex;"><i class="fas fa-building text-muted" style="font-size: 0.7rem;"></i></span>');
            }
            $('#selectedMinistryDisplay').append('<span>' + ministryName + '</span>');
        }

        // Restore ministry display if already selected (from model)
        var selectedMinistryCode = $('#ministrySelect').val();
        if (selectedMinistryCode) {
            var $selectedMinistryItem = $('.ministry-item[data-ministry-code="' + selectedMinistryCode + '"]');
            if ($selectedMinistryItem.length > 0) {
                var ministryName = $selectedMinistryItem.data('ministry-name');
                var ministryLogo = $selectedMinistryItem.data('ministry-logo');
                updateMinistryDisplay(selectedMinistryCode, ministryName, ministryLogo);
            }
        }

        // Make ministry dropdown readonly for ministry users
        var isMinistryUser = @Html.Raw(Json.Serialize(ViewBag.IsMinistryUser ?? false));
        if (isMinistryUser) {
            // Add readonly styling and prevent changes
            $('#ministrySelect').css({
                'background-color': '#e9ecef',
                'cursor': 'not-allowed',
                'pointer-events': 'none'
            }).addClass('readonly-select');

            // Also add a visual indicator
            $('#ministrySelect').closest('.mb-4').append(
                '<small class="form-text text-muted"><i class="fas fa-lock me-1"></i>' + localizedStrings.ministryAutoAssigned + '</small>'
            );
        }
    });

    let selectedDonors = [];
    let donorFundingData = {};

    // Currency symbols mapping
    const currencySymbols = {
        'USD': '$',
        'EUR': '\u20AC',
        'SYP': 'SYP '
    };

    // Get current currency symbol
    function getCurrencySymbol() {
        const currency = $('#currencySelect').val() || 'USD';
        return currencySymbols[currency] || '$';
    }

    // Update all currency symbol displays
    function updateCurrencySymbols() {
        const symbol = getCurrencySymbol();
        $('.currency-symbol').text(symbol);
        calculateDonorTotals();
    }

    // Handle currency selection change
    $('#currencySelect').on('change', function() {
        updateCurrencySymbols();
        toggleExchangeRateSection($(this).val());
    });

    // ========== EXCHANGE RATE SECTION ==========

    function toggleExchangeRateSection(currency) {
        if (currency === 'SYP') {
            $('#exchangeRateSection').show();
            // Auto-fetch if no rate is already filled in
            if (!$('#exchangeRateInput').val()) {
                fetchExchangeRate();
            }
        } else {
            $('#exchangeRateSection').hide();
        }
    }

    function fetchExchangeRate() {
        const btn = $('#fetchRateBtn');
        const errorDiv = $('#fetchRateError');
        errorDiv.hide();
        btn.prop('disabled', true).html('<i class="fas fa-spinner fa-spin me-1"></i>@Localizer["Fetching..."]');

        fetch('https://open.er-api.com/v6/latest/USD')
            .then(response => response.json())
            .then(data => {
                if (data && data.rates && data.rates.SYP) {
                    const rate = Math.round(data.rates.SYP * 100) / 100;
                    const today = new Date().toISOString().split('T')[0];
                    $('#exchangeRateInput').val(rate);
                    $('#exchangeRateDateInput').val(today);
                    $('#exchangeRateDateDisplay').text(today);
                    $('#exchangeRateMeta').show();
                    $('#exchangeRateSourceBadge').show();
                } else {
                    setManualRateDate();
                    errorDiv.text('@Localizer["Could not fetch live rate. Please enter manually."]').show();
                }
            })
            .catch(() => {
                setManualRateDate();
                errorDiv.text('@Localizer["Could not fetch live rate. Please enter manually."]').show();
            })
            .finally(() => {
                btn.prop('disabled', false).html('<i class="fas fa-sync-alt me-1"></i>@Localizer["Fetch Rate"]');
            });
    }

    function setManualRateDate() {
        const today = new Date().toISOString().split('T')[0];
        $('#exchangeRateDateInput').val(today);
        $('#exchangeRateDateDisplay').text(today);
        $('#exchangeRateMeta').show();
        $('#exchangeRateSourceBadge').hide();
    }

    // When user manually edits the rate, record today as the date
    $('#exchangeRateInput').on('input', function() {
        if ($(this).val()) {
            setManualRateDate();
        }
        updateUsdEquivalent();
    });

    $('#fetchRateBtn').on('click', function() {
        fetchExchangeRate();
    });

    // Recalculate when budget changes
    $('[name="EstimatedBudget"]').on('input change', updateUsdEquivalent);

    // Arabic words conversion for the estimated budget
    function convertNumberToArabicWords(num) {
        if (!isFinite(num) || num < 1000) {
            return '';
        }
        num = Math.floor(num);
        const billions = Math.floor(num / 1000000000);
        const afterBillions = num % 1000000000;
        const millions = Math.floor(afterBillions / 1000000);
        const afterMillions = afterBillions % 1000000;
        const thousands = Math.floor(afterMillions / 1000);
        const remainder = afterMillions % 1000;

        const parts = [];
        if (billions > 0) {
            parts.push(billions === 1 ? 'مليار' : billions + ' مليار');
        }
        if (millions > 0) {
            parts.push(millions === 1 ? 'مليون' : millions + ' مليون');
        }
        if (thousands > 0) {
            if (thousands === 1) {
                parts.push('ألف');
            } else if (thousands === 10) {
                parts.push('عشرة آلاف');
            } else {
                parts.push(thousands + ' ألف');
            }
        }
        if (remainder > 0) {
            parts.push(remainder.toString());
        }
        return parts.join(' و');
    }

    function updateBudgetArabicWords() {
        const value = parseFloat($('[name="EstimatedBudget"]').val());
        const $box = $('#estimatedBudgetArabicWords');
        const $val = $('#estimatedBudgetArabicWordsValue');
        if (isNaN(value) || value < 1000) {
            $box.hide();
            $val.text('');
            return;
        }
        $val.text(convertNumberToArabicWords(value));
        $box.show();
    }

    $('[name="EstimatedBudget"]').on('input change', updateBudgetArabicWords);
    updateBudgetArabicWords();

    function updateUsdEquivalent() {
        const budget = parseFloat($('[name="EstimatedBudget"]').val());
        const rate = parseFloat($('#exchangeRateInput').val());
        if (budget > 0 && rate > 0) {
            const usd = budget / rate;
            const formatted = usd.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 });
            const budgetFormatted = budget.toLocaleString('en-US');
            const rateFormatted = rate.toLocaleString('en-US');
            $('#usdEquivalentValue').text(formatted);
            $('#usdEquivalentFormula').text(budgetFormatted + ' SYP ÷ ' + rateFormatted + ' = ' + formatted + ' USD');
            $('#usdEquivalentBox').show();
        } else {
            $('#usdEquivalentBox').hide();
        }
    }

    // Initialize on page load
    toggleExchangeRateSection($('#currencySelect').val());
    // ===========================================

    // Add donor button click handler
    $('#addDonorBtn').click(function() {
        const $selectedOption = $('#availableDonorSelect option:selected');
        const donorValue = $('#availableDonorSelect').val();
        const donorText = $selectedOption.text();
        const isInvestmentBudget = !!$selectedOption.data('investment-budget');

        if (!donorValue || donorValue === '') {
            alert(localizedStrings.pleaseSelectDonorFirst);
            return;
        }

        // Check if donor is already added
        if (selectedDonors.some(d => d.value === donorValue)) {
            alert(localizedStrings.donorAlreadyAdded);
            return;
        }

        // Add donor to selected list
        selectedDonors.push({
            value: donorValue,
            text: donorText,
            isInvestmentBudget: isInvestmentBudget
        });

        // Initialize percentage
        donorFundingData[donorValue] = 0;

        // Update UI
        updateDonorFundingSection();
        updateSelectedDonorsInput();
        updatePhaseSelectionForDonors();
        updateDonorSelectRestrictions();

        // Reset dropdown
        $('#availableDonorSelect').val('');
    });

    // Update the donor funding section
    function updateDonorFundingSection() {
        const donorFundingSection = $('#donorFundingSection');
        const donorContributionList = $('#donorContributionList');

        if (selectedDonors.length === 0) {
            donorFundingSection.hide();
            return;
        }

        donorFundingSection.show();
        donorContributionList.empty();

        const symbol = getCurrencySymbol();
        const budget = parseFloat($('[name="EstimatedBudget"]').val()) || 0;

        // Create cards with sliders for each selected donor
        selectedDonors.forEach(donor => {
            const currentPercentage = donorFundingData[donor.value] || 0;

            const donorHtml =
                '<div class="card mb-3 donor-funding-card" data-donor="' + donor.value + '">' +
                    '<div class="card-body">' +
                        '<div class="row align-items-center">' +
                            '<div class="col-md-3">' +
                                '<h6 class="mb-1">' +
                                    '<i class="fas fa-hand-holding-usd me-2"></i>' + donor.text +
                                '</h6>' +
                                '<small class="text-muted">' + localizedStrings.fundingContribution + '</small>' +
                            '</div>' +
                            '<div class="col-md-2">' +
                                '<label class="form-label small mb-1">' + localizedStrings.percentage + '</label>' +
                                '<div class="input-group input-group-sm">' +
                                    '<input type="number" class="form-control donor-percentage-input" data-donor="' + donor.value + '" min="0" max="100" step="0.1" value="' + currentPercentage.toFixed(1) + '">' +
                                    '<span class="input-group-text">%</span>' +
                                '</div>' +
                            '</div>' +
                            '<div class="col-md-4">' +
                                '<label class="form-label small mb-1 d-none d-md-block">&nbsp;</label>' +
                                '<input type="range" class="form-range donor-slider" data-donor="' + donor.value + '" min="0" max="100" step="0.1" value="' + currentPercentage + '">' +
                                '<div class="d-flex justify-content-between small text-muted">' +
                                    '<span>0%</span>' +
                                    '<span>100%</span>' +
                                '</div>' +
                            '</div>' +
                            '<div class="col-md-2">' +
                                '<label class="form-label small mb-1">' + localizedStrings.amount + '</label>' +
                                '<div class="input-group input-group-sm">' +
                                    '<span class="input-group-text currency-symbol-badge">' + symbol + '</span>' +
                                    '<input type="number" class="form-control donor-amount-input" data-donor="' + donor.value + '" min="0" step="0.01" value="' + ((budget * currentPercentage) / 100).toFixed(2) + '">' +
                                '</div>' +
                            '</div>' +
                            '<div class="col-md-1">' +
                                '<label class="form-label small mb-1 d-none d-md-block">&nbsp;</label>' +
                                '<button type="button" class="btn btn-outline-danger btn-sm remove-donor-btn" data-donor="' + donor.value + '">' +
                                    '<i class="fas fa-times"></i>' +
                                '</button>' +
                            '</div>' +
                        '</div>' +
                    '</div>' +
                '</div>';
            donorContributionList.append(donorHtml);
        });

        calculateDonorTotals();
    }

    // Handle slider changes
    $(document).on('input', '.donor-slider', function() {
        const donorValue = String($(this).data('donor'));
        const percentage = parseFloat($(this).val()) || 0;

        donorFundingData[donorValue] = percentage;

        // Update text input to match slider
        $(`.donor-percentage-input[data-donor="${donorValue}"]`).val(percentage.toFixed(1));

        calculateDonorTotals();
    });

    // Handle text input changes
    $(document).on('input', '.donor-percentage-input', function() {
        const donorValue = String($(this).data('donor'));
        let percentage = parseFloat($(this).val()) || 0;

        // Clamp value between 0 and 100
        if (percentage < 0) percentage = 0;
        if (percentage > 100) percentage = 100;

        donorFundingData[donorValue] = percentage;

        // Update slider to match text input
        $(`.donor-slider[data-donor="${donorValue}"]`).val(percentage);

        calculateDonorTotals();
    });

    // Handle text input blur to format the value
    $(document).on('blur', '.donor-percentage-input', function() {
        const donorValue = String($(this).data('donor'));
        let percentage = parseFloat($(this).val()) || 0;

        // Clamp and format value
        if (percentage < 0) percentage = 0;
        if (percentage > 100) percentage = 100;

        $(this).val(percentage.toFixed(1));
    });

    // Handle amount input changes → derive percentage from amount
    $(document).on('input', '.donor-amount-input', function() {
        const donorValue = String($(this).data('donor'));
        const amount = parseFloat($(this).val()) || 0;
        const budget = parseFloat($('[name="EstimatedBudget"]').val()) || 0;

        let percentage = budget > 0 ? (amount / budget) * 100 : 0;
        if (percentage < 0) percentage = 0;
        if (percentage > 100) percentage = 100;

        donorFundingData[donorValue] = percentage;

        // Sync percentage input and slider (without triggering their handlers)
        $(`.donor-percentage-input[data-donor="${donorValue}"]`).val(percentage.toFixed(1));
        $(`.donor-slider[data-donor="${donorValue}"]`).val(percentage);

        calculateDonorTotals();
    });

    // Handle remove donor button
    $(document).on('click', '.remove-donor-btn', function() {
        const donorValue = String($(this).data('donor'));

        // Remove from selected donors array
        selectedDonors = selectedDonors.filter(d => d.value !== donorValue);

        // Remove from funding data
        delete donorFundingData[donorValue];

        // Update UI
        updateDonorFundingSection();
        updateSelectedDonorsInput();
        updatePhaseSelectionForDonors();
        updateDonorSelectRestrictions();
    });

    // Toggle phase section: show ONLY when "موازنة أستثمارية" is selected; otherwise hide and clear checkboxes
    function updatePhaseSelectionForDonors() {
        const hasInvestmentBudget = selectedDonors.some(d => !!d.isInvestmentBudget);
        if (hasInvestmentBudget) {
            $('#phaseSelectionSection').show();
        } else {
            $('#phaseSelectionSection').hide();
            // Uncheck all phase checkboxes so they don't get submitted
            $('.phase-checkbox').prop('checked', false);
        }
    }

    // Restrict donor selection based on current state:
    // - If "موازنة أستثمارية" is selected → hide the Add button (no more donors allowed)
    // - If any other donor is selected → hide "موازنة أستثمارية" option from dropdown
    function updateDonorSelectRestrictions() {
        const hasInvestmentBudget = selectedDonors.some(d => !!d.isInvestmentBudget);
        const hasOtherDonors = selectedDonors.some(d => !d.isInvestmentBudget);

        // Hide add button when investment budget is selected
        if (hasInvestmentBudget) {
            $('#addDonorBtn').hide();
            $('#availableDonorSelect').hide();
        } else {
            $('#addDonorBtn').show();
            $('#availableDonorSelect').show();
        }

        // Hide/show "موازنة أستثمارية" option based on whether other donors exist
        $('#availableDonorSelect option[data-investment-budget]').toggle(!hasOtherDonors);
    }

    // Update the hidden select input for form submission
    function updateSelectedDonorsInput() {
        const hiddenSelect = $('#selectedDonorsInput');
        hiddenSelect.empty();

        selectedDonors.forEach(donor => {
            hiddenSelect.append(`<option value="${donor.value}" selected>${donor.text}</option>`);
        });
    }

    // Calculate totals and update displays
    function calculateDonorTotals() {
        const budget = parseFloat($('[name="EstimatedBudget"]').val()) || 0;
        const symbol = getCurrencySymbol();
        let totalPercentage = 0;
        let totalAmount = 0;

        // Update individual donor amounts and calculate totals
        selectedDonors.forEach(donor => {
            const percentage = donorFundingData[donor.value] || 0;
            const amount = (budget * percentage) / 100;

            totalPercentage += percentage;
            totalAmount += amount;

            // Update the amount input for this donor (only if user is not currently editing it)
            const $amountInput = $(`.donor-amount-input[data-donor="${donor.value}"]`);
            if (!$amountInput.is(':focus')) {
                $amountInput.val(amount.toFixed(2));
            }
            // Update currency symbol badge
            $('.currency-symbol-badge').text(symbol);
        });

        // Update total displays
        $('#totalPercentage').text(totalPercentage.toFixed(1));
        $('#totalFundingAmount').text(totalAmount.toFixed(2));

        // Update status message
        const statusElement = $('#percentageStatus');
        if (totalPercentage > 100) {
            statusElement.text('Error: Exceeds 100%').removeClass('text-success text-muted text-warning').addClass('text-danger');
        } else if (totalPercentage === 100) {
            statusElement.text(localizedStrings.fullyFunded + ' (100%)').removeClass('text-warning text-danger text-muted').addClass('text-success');
        } else if (totalPercentage > 0) {
            statusElement.text(localizedStrings.partiallyFunded + ' (' + totalPercentage.toFixed(1) + '%)').removeClass('text-success text-danger text-muted').addClass('text-warning');
        } else {
            statusElement.text(localizedStrings.cannotExceed100).removeClass('text-success text-warning text-danger').addClass('text-muted');
        }

        // Update hidden field with funding data
        $('#donorFundingData').val(JSON.stringify(donorFundingData));
    }

    // Update calculations when budget changes
    $('[name="EstimatedBudget"]').on('input change', function() {
        calculateDonorTotals();
    });

    // Enhanced form validation before submission
    $('form.needs-validation').on('submit', function (e) {
        let isValid = true;
        let errorMessages = [];

        // Helper function to add validation error styling
        function addFieldError(selector, containerSelector) {
            $(selector).addClass('is-invalid');
            if (containerSelector) {
                $(containerSelector).addClass('has-validation-error');
            }
        }

        // Helper function to remove validation error styling
        function removeFieldError(selector, containerSelector) {
            $(selector).removeClass('is-invalid');
            if (containerSelector) {
                $(containerSelector).removeClass('has-validation-error');
            }
        }

        // Clear all previous validation errors
        $('.is-invalid').removeClass('is-invalid');
        $('.has-validation-error').removeClass('has-validation-error');

        // Check if locations are selected (unless "Entire Country" is checked)
        const isEntireCountry = $("#entireCountryCheckbox").is(':checked');
        if (!isEntireCountry && selections.length === 0) {
            errorMessages.push(localizedStrings.atLeastOneLocation);
            $('#location-selector').closest('.mb-4').addClass('has-validation-error');
            isValid = false;
        }

        // Check if sectors are selected
        const selectedSectors = $('select[name="Sectors"]').val();
        if (!selectedSectors || selectedSectors.length === 0) {
            errorMessages.push(localizedStrings.atLeastOneSector);
            $('#sectors-validation-error').text(localizedStrings.atLeastOneSector).show();
            addFieldError('select[name="Sectors"]', '#sectors-container');
            isValid = false;
        } else {
            $('#sectors-validation-error').hide();
            removeFieldError('select[name="Sectors"]', '#sectors-container');
        }

        // Check if project manager is selected
        const projectManager = $('select[name="ProjectManagerCode"]').val();
        if (!projectManager) {
            errorMessages.push(localizedStrings.projectManagerMustBeSelected);
            addFieldError('select[name="ProjectManagerCode"]', 'select[name="ProjectManagerCode"]').closest('.mb-4');
            $('select[name="ProjectManagerCode"]').addClass('is-invalid').closest('.mb-4').addClass('has-validation-error');
            isValid = false;
        }

        // Check if supervisor is selected
        const supervisor = $('select[name="SuperVisorCode"]').val();
        if (!supervisor) {
            errorMessages.push(localizedStrings.supervisorMustBeSelected);
            $('select[name="SuperVisorCode"]').addClass('is-invalid').closest('.mb-4').addClass('has-validation-error');
            isValid = false;
        }

        // Check if ministry is selected
        const ministry = $('input[name="MinistryCode"]').val();
        if (!ministry) {
            errorMessages.push(localizedStrings.ministryMustBeSelected);
            $('#ministryDropdown').addClass('is-invalid').closest('.mb-4').addClass('has-validation-error');
            isValid = false;
        }

        // Check date range
        const startDate = new Date($('input[name="StartDate"]').val());
        const endDate = new Date($('input[name="EndDate"]').val());
        if (startDate && endDate && endDate <= startDate) {
            errorMessages.push(localizedStrings.endDateMustBeAfterStart);
            $('input[name="StartDate"]').addClass('is-invalid').closest('.col-md-6').addClass('has-validation-error');
            $('input[name="EndDate"]').addClass('is-invalid').closest('.col-md-6').addClass('has-validation-error');
            isValid = false;
        }

        // Check donor funding total if donors are selected (should not exceed 100%)
        if (selectedDonors.length > 0) {
            const totalPercentage = parseFloat($('#totalPercentage').text());
            if (totalPercentage > 100) {
                errorMessages.push(localizedStrings.donorFundingCannotExceed);
                $('#donorFundingSection').addClass('has-validation-error');
                isValid = false;
            }
        }

        if (!isValid) {
            e.preventDefault();
            // Show all error messages
            const errorHtml = '<div id="form-validation-alert" class="alert alert-danger alert-dismissible fade show mt-3" role="alert">' +
                '<strong><i class="fas fa-exclamation-triangle me-2"></i>' + localizedStrings.pleaseFixFollowingIssues + '</strong>' +
                '<ul class="mb-0 mt-2">' +
                errorMessages.map(function(msg) { return '<li>' + msg + '</li>'; }).join('') +
                '</ul>' +
                '<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="' + localizedStrings.close + '"></button>' +
                '</div>';

            // Remove existing form validation alert and add new one
            $('#form-validation-alert').remove();
            $('#main-form-body').prepend(errorHtml);

            // Scroll to first error field
            var $firstError = $('.has-validation-error').first();
            if ($firstError.length > 0) {
                $('html, body').animate({
                    scrollTop: $firstError.offset().top - 120
                }, 500);
                // Focus on the first invalid input
                $firstError.find('.is-invalid, input, select').first().focus();
            } else {
                // Fallback: scroll to top of form
                $('html, body').animate({
                    scrollTop: $('#main-form-body').offset().top - 100
                }, 500);
            }

            return false;
        }
    });

    // Function to restore form data after validation failure
    function restoreFormData() {
        // Get preserved data from ViewBag
        var selectedSectorCodes = @Html.Raw(Json.Serialize(ViewBag.SelectedSectorCodes ?? new List<string>()));
        var selectedDonorCodes = @Html.Raw(Json.Serialize(ViewBag.SelectedDonorCodes ?? new List<string>()));
        var locationSelections = @Html.Raw(Json.Serialize(ViewBag.LocationSelections ?? ""));
        var donorFundingBreakdown = @Html.Raw(Json.Serialize(ViewBag.DonorFundingBreakdown ?? ""));

        // Restore selected sectors
        if (selectedSectorCodes && selectedSectorCodes.length > 0) {
            selectedSectorCodes.forEach(function(code) {
                $('select[name="Sectors"] option[value="' + code + '"]').prop('selected', true);
            });
        }

        // Restore location selections
        if (locationSelections && locationSelections !== '') {
            try {
                if (typeof locationSelections === 'string') {
                    selections = JSON.parse(locationSelections);
                } else {
                    selections = locationSelections;
                }
                restoreLocationSelections();
            } catch (e) {
                console.log('Error parsing location selections:', e);
            }
        }

        // Restore donor selections and funding data
        if (selectedDonorCodes && selectedDonorCodes.length > 0) {
            restoreDonorSelections(selectedDonorCodes, donorFundingBreakdown);
        }
    }

    // Function to restore location selections in the UI
    function restoreLocationSelections() {
        if (selections && selections.length > 0) {
            selections.forEach(function(item) {
                // Use names if available, otherwise use codes
                var govDisplay = item.GovernorateName || ('Governorate ' + item.GovernorateCode);
                var distDisplay = item.DistrictName || ('District ' + item.DistrictCode);
                var subDisplay = item.SubDistrictName || ('SubDistrict ' + item.SubDistrictCode);
                var commDisplay = item.CommunityName || ('Community ' + item.CommunityCode);

                // Create list item display
                const $li = $(
                    '<li class="list-group-item">' +
                        '<span>' + govDisplay + ' > ' + distDisplay + ' > ' + subDisplay + ' > ' + commDisplay + '</span>' +
                        '<button type="button" class="btn btn-sm btn-outline-danger btn-clear">' +
                            '<i class="fas fa-times"></i> ' + localizedStrings.clear +
                        '</button>' +
                    '</li>'
                );

                // Attach remove handler
                $li.find(".btn-clear").click(function () {
                    selections = selections.filter(x =>
                        !(x.GovernorateCode === item.GovernorateCode &&
                          x.DistrictCode === item.DistrictCode &&
                          x.SubDistrictCode === item.SubDistrictCode &&
                          x.CommunityCode === item.CommunityCode)
                    );
                    $li.remove();
                    $("#location-data").val(JSON.stringify(selections));
                });

                $("#selected-locations").append($li);
            });

            // Update hidden input
            $("#location-data").val(JSON.stringify(selections));
        }
    }

    // Function to restore donor selections
    function restoreDonorSelections(donorCodes, fundingBreakdown) {
        selectedDonors = [];
        donorFundingData = {};

        // Parse funding breakdown if available
        var fundingData = {};
        if (fundingBreakdown && fundingBreakdown !== '') {
            try {
                if (typeof fundingBreakdown === 'string') {
                    fundingData = JSON.parse(fundingBreakdown);
                } else {
                    fundingData = fundingBreakdown;
                }
            } catch (e) {
                console.log('Error parsing donor funding breakdown:', e);
            }
        }

        // Get donor text from select options
        donorCodes.forEach(function(code) {
            var donorText = $('#availableDonorSelect option[value="' + code + '"]').text();
            if (donorText) {
                selectedDonors.push({
                    value: code,
                    text: donorText
                });
                donorFundingData[code] = fundingData[code] || 0;
            }
        });

        // Update UI
        updateDonorFundingSection();
        updateSelectedDonorsInput();
        updatePhaseSelectionForDonors();
        updateDonorSelectRestrictions();
    }

    // =====================
    // FILE UPLOAD MANAGEMENT
    // =====================

    let selectedFiles = [];
    const fileInput = document.getElementById('fileInput');
    const fileUploadArea = document.getElementById('fileUploadArea');
    const selectedFilesList = document.getElementById('selectedFilesList');

    // Handle file input change
    if (fileInput) {
        fileInput.addEventListener('change', function(e) {
            handleFiles(e.target.files);
        });
    }

    // Handle drag and drop
    if (fileUploadArea) {
        fileUploadArea.addEventListener('dragover', function(e) {
            e.preventDefault();
            e.stopPropagation();
            this.classList.add('dragover');
        });

        fileUploadArea.addEventListener('dragleave', function(e) {
            e.preventDefault();
            e.stopPropagation();
            this.classList.remove('dragover');
        });

        fileUploadArea.addEventListener('drop', function(e) {
            e.preventDefault();
            e.stopPropagation();
            this.classList.remove('dragover');
            handleFiles(e.dataTransfer.files);
        });

        fileUploadArea.addEventListener('click', function(e) {
            if (e.target.tagName !== 'BUTTON' && e.target.tagName !== 'INPUT') {
                fileInput.click();
            }
        });
    }

    function handleFiles(files) {
        for (let i = 0; i < files.length; i++) {
            const file = files[i];

            // Check if file is already added
            const exists = selectedFiles.some(f => f.name === file.name && f.size === file.size);
            if (!exists) {
                selectedFiles.push(file);
            }
        }
        updateFilesList();
        updateFileInput();
    }

    function removeFile(index) {
        selectedFiles.splice(index, 1);
        updateFilesList();
        updateFileInput();
    }

    function clearAllFiles() {
        selectedFiles = [];
        updateFilesList();
        updateFileInput();
    }

    function updateFilesList() {
        if (!selectedFilesList) return;

        if (selectedFiles.length === 0) {
            selectedFilesList.innerHTML = '';
            return;
        }

        let html = '';

        // Summary bar
        const totalSize = selectedFiles.reduce((sum, f) => sum + f.size, 0);
        html += `
            <div class="files-summary">
                <span class="files-summary-text">
                    <i class="fas fa-paperclip me-2"></i>
                    ${selectedFiles.length} ${localizedStrings.filesSelected} (${formatFileSize(totalSize)})
                </span>
                <button type="button" class="btn btn-outline-danger btn-sm clear-all-btn" onclick="clearAllFiles()">
                    <i class="fas fa-trash me-1"></i>${localizedStrings.clearAll}
                </button>
            </div>
        `;

        // File items
        selectedFiles.forEach((file, index) => {
            const iconClass = getFileIconClass(file.name);
            html += `
                <div class="file-item">
                    <div class="file-item-info">
                        <div class="file-item-icon ${iconClass}">
                            <i class="fas ${getFileIcon(file.name)}"></i>
                        </div>
                        <div class="file-item-details">
                            <div class="file-item-name" title="${file.name}">${file.name}</div>
                            <div class="file-item-size">${formatFileSize(file.size)}</div>
                        </div>
                    </div>
                    <button type="button" class="file-item-remove" onclick="removeFile(${index})" title="${localizedStrings.remove}">
                        <i class="fas fa-times"></i>
                    </button>
                </div>
            `;
        });

        selectedFilesList.innerHTML = html;
    }

    function updateFileInput() {
        // Create a new DataTransfer object to set files on input
        const dt = new DataTransfer();
        selectedFiles.forEach(file => dt.items.add(file));
        fileInput.files = dt.files;
    }

    function getFileIconClass(filename) {
        const ext = filename.split('.').pop().toLowerCase();
        if (ext === 'pdf') return 'pdf';
        if (['doc', 'docx'].includes(ext)) return 'doc';
        if (['xls', 'xlsx'].includes(ext)) return 'xls';
        if (['jpg', 'jpeg', 'png', 'gif'].includes(ext)) return 'img';
        return 'default';
    }

    function getFileIcon(filename) {
        const ext = filename.split('.').pop().toLowerCase();
        if (ext === 'pdf') return 'fa-file-pdf';
        if (['doc', 'docx'].includes(ext)) return 'fa-file-word';
        if (['xls', 'xlsx'].includes(ext)) return 'fa-file-excel';
        if (['jpg', 'jpeg', 'png', 'gif'].includes(ext)) return 'fa-file-image';
        return 'fa-file';
    }

    function formatFileSize(bytes) {
        if (bytes === 0) return '0 Bytes';
        const k = 1024;
        const sizes = ['Bytes', 'KB', 'MB', 'GB'];
        const i = Math.floor(Math.log(bytes) / Math.log(k));
        return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + ' ' + sizes[i];
    }

    (function () {
        document.querySelectorAll('.month-year-picker').forEach(function (picker) {
            var hidden = picker.querySelector('input[type="hidden"]');
            var monthSel = picker.querySelector('.mypicker-month');
            var yearInput = picker.querySelector('.mypicker-year');
            if (hidden.value) {
                var parts = hidden.value.split('-');
                if (parts.length >= 2) {
                    yearInput.value = parseInt(parts[0], 10);
                    monthSel.value = parseInt(parts[1], 10);
                }
            }
            function sync() {
                var m = monthSel.value, y = yearInput.value;
                if (m && y) hidden.value = String(parseInt(y)).padStart(4, '0') + '-' + String(parseInt(m)).padStart(2, '0');
            }
            monthSel.addEventListener('change', sync);
            yearInput.addEventListener('change', sync);
            yearInput.addEventListener('input', sync);
        });
    })();

</script>
